"""hw_agent agents package.

Each subpackage is one agent (researcher, pcb_designer) implemented as
plain Python modules. The matching .claude/agents/<name>.md file is the
agent definition the harness loads; this package holds the callable
helpers those agents drive via MCP/Bash tools.
"""
