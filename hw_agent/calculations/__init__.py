"""Component calculations — math libraries referenced from templates.

Each module hosts the calculation functions for a component category.
Templates list calculations by direct import (not by string name) so refactors
are catchable by the type checker / IDE.
"""
